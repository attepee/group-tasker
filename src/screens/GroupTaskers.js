import * as React from 'react';
import { Text, View } from 'react-native';

export function GroupTaskers({ navigation }) {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Group taskers</Text>
        </View>
    );
}